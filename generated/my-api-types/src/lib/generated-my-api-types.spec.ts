import { generatedMyApiTypes } from './generated-my-api-types';

describe('generatedMyApiTypes', () => {
  it('should work', () => {
    expect(generatedMyApiTypes()).toEqual('generated-my-api-types');
  });
});
